# OpenAI API 키 (https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-your-api-key-here
# 사용할 GPT 모델 (기본: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# 이미지 생성 모델 (기본: gpt-image-2)
OPENAI_IMAGE_MODEL=gpt-image-2

# 서버 포트 (기본: 8080)
PORT=8080
